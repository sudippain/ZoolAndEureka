eureka:
    client:
        fetchRegistry: false
        registerWithEureka: false
        serviceUrl:
            defaultZone: 'http://${eureka.instance.hostname}:${server.port}/eureka/'
    instance:
        hostname: localhost
server:
    port: 8183
spring:
    application:
        name: service1
